//FizzBuzz program dde3cn Drake Ewing
#include<stdio.h>

int main(void)
  {
    int x;
    for(x=0;x<101;++x)
      {
        if( x % 3 == 0);
          printf("Fizz");
        if(x % 5 == 0);
          printf("Buzz");
        if((x % 3 != 0) && (x % 5 != 0))
          printf("number = %d",x)
      }
      printf("/n")
  }
